# Useful utils

# Reformat codes/ Make it readable and clear
## Short cut : ctrl + shift + A with block
library(styler)
library(prettycode)
# or
library(formatR)
# tidy_eval("file path")
# tidy_eval("C:/Users/PC_name/Desktop/myfile.R")
usage(glm, width = 40) # args(glm)
